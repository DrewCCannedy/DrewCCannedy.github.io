name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout site
        uses: actions/checkout@v5

      - name: Checkout dooby-engine
        uses: actions/checkout@v5
        with:
          repository: DrewCCannedy/dooby-engine
          token: ${{ secrets.DOOBY_ENGINE_TOKEN }}
          path: _dooby-engine

      - name: Checkout tarkov-trading-card
        uses: actions/checkout@v5
        with:
          repository: DrewCCannedy/tarkov-trading-card
          token: ${{ secrets.DOOBY_ENGINE_TOKEN }}
          path: _tarkov-trading-card

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            _dooby-engine/package-lock.json
            _tarkov-trading-card/package-lock.json

      - name: Build dooby-engine
        run: |
          cd _dooby-engine
          npm ci
          npm run build

      - name: Copy dooby-engine dist to site
        run: |
          mkdir -p dooby-engine
          cp -r _dooby-engine/dist/* dooby-engine/

      - name: Build tarkov-trading-card
        run: |
          cd _tarkov-trading-card
          npm ci
          npm run build

      - name: Copy tarkov-trading-card dist to site
        run: |
          mkdir -p tarkov-trading-card
          cp -r _tarkov-trading-card/apps/web/dist/* tarkov-trading-card/

      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: .

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
